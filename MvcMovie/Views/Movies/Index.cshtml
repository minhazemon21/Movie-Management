@model IEnumerable<MvcMovie.Models.Movie>


@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";

}

<div class="row">
    <h2>Movie Information</h2>


    <p>
        @Html.ActionLink("Create New", "Create")

        @using (Html.BeginForm("Index", "Movies", FormMethod.Get))
        {
        <p>
            Genre: @Html.DropDownList("movieGenre", "All")


        </p>
        <p>Title: @Html.TextBox("SearchString")</p>
        <p><input type="submit" value="Filter"  class="btn-primary"/></p>
    }
        </p>
        <table class="table">
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Id)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Title)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Description)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.ReleaseDate)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Genre)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Price)
                </th>
                <th>
                    @Html.Label("Action Name")
                </th>
            </tr>

            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Id)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Title)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Description)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.ReleaseDate)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Genre)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Price)
                    </td>
                    <td>
                        @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                        @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                        @Html.ActionLink("Delete", "Delete", new { id = item.Id })
                    </td>
                </tr>
            }

        </table>

    <button class="btn btn-primary" id="btnref" onclick="window.location.reload()">Refresh</button>
    </div>
<script type="text/javascript">
    //$(document).ready(function () {
    //    $("#btnShow").click(function (e) {
    //        location.reload();
    //    });
    //    //$(document).on("click", "#btnref", function () {

    //    //});
    //});
    //$("#datecheckbox").change(function () {
    //    var datecheckStatus = $("#datecheckbox").is(":checked") == true ? 1 : 0;
    //    if (datecheckStatus == 1) {
    //        $("#startendaction").show();
    //    }
    //    else {
    //        $("#startendaction").hide();
    //    }

    //});
    
    
</script>